In this step a reorganization of exec code is done. The copy of the program
name from user space is done with a new strncpy_from_user routine.
The same routine is used to semplify the implementation of syscall print.

Files modified:
---------------
exec.c
uaccess.h
usercopy.c (new)
console.c
